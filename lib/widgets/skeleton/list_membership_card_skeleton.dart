import 'package:flutter/widgets.dart';

class ListMemcardSkeleton extends StatelessWidget {
  const ListMemcardSkeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
