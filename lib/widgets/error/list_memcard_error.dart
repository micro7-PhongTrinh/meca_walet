import 'package:flutter/widgets.dart';

class ListMemcardError extends StatelessWidget {
  const ListMemcardError({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
