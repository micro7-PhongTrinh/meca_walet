import 'package:flutter/material.dart';
import 'package:meca_wallet/constants/colors.dart';

class SuggestItemSkeleton extends StatelessWidget {
  const SuggestItemSkeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 60, color: kFillColorPrimary);
  }
}
