import 'package:flutter_test/flutter_test.dart';

main() {
  setUp(() async {});

  testWidgets('Verify UI of membership card screen', (widgetTester) async {

    //phan membership card screen se gom 2 phan: list card, recent activity
  });
}
