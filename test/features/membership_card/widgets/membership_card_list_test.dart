import 'package:flutter_test/flutter_test.dart';

main() {
  setUp(() async {});

  testWidgets('Verify UI of membership card list', (widgetTester) async {});
  testWidgets('Verify membership card list will wrap by a GetMembershipCardCubit', (widgetTester) async {});
  testWidgets('Verify widget will call to Cubit get data method when initial', (widgetTester) async {});
  testWidgets('Verify rendering when in specific bloc', (widgetTester) async {});

}
